# This project uses the PUBLIC npm registry.
# (Your global npm points at a private AWS CodeArtifact registry that requires an
#  auth token; without it, installs hang. This override keeps this app buildable
#  regardless of corporate npm auth state.)
registry=https://registry.npmjs.org/
audit=false
fund=false
fetch-retries=5
fetch-timeout=180000
